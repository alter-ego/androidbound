<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="solutions.alterego.androidbound.example">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-sdk tools:overrideLibrary="com.codemonkeylabs.fpslibrary"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_android_white_48dp"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".MainBindingActivity"
                android:label="@string/app_name">
        </activity>

        <activity
                android:name=".ListViewActivity"
                android:label="@string/app_name">
        </activity>

        <activity
                android:name=".ListViewWithObjectsActivity"
                android:label="@string/title_activity_LVWithObjects"/>

        <activity
                android:name=".RecyclerViewWithObjectsActivity"
                android:label="@string/title_activity_RVWithObjects"/>
        <activity
                android:name=".ListItemDetailActivity"
                android:label="@string/app_name">
        </activity>
    </application>
</manifest>
